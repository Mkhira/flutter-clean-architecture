import 'package:flutter/material.dart';

/// Raw color tokens / seed colors. Widgets consume the [ColorScheme] derived
/// from these, not these constants directly.
abstract class AppColors {
  static const Color seed = Color(0xFF3A6EA5);
}
